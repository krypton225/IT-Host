interface IMainButton {
    text: string;
    color: string;
    bg: string;
    rounded: boolean;
    padding?: string;
    halfWidth?: boolean;
    setCustomWidth?: string;
}

export default IMainButton;